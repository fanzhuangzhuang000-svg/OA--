# 172 服务器 QA 测试报告

**测试时间**: 2026-06-19 21:05
**测试范围**: 172.20.0.139（测试平台，nbcy/admin123） - **严格未访问 152**

---

## 🎯 总览

| 测试项 | 总数 | 通过 | 失败 | 通过率 |
|--------|------|------|------|--------|
| **数据清理** | 35 业务表 | 35 | 0 | **100%** |
| **API 联调** | 49 端点 | 45 | 4 | **91.8%** |
| **前端路由** | 32 路由 | 32 | 0 | **100%** |
| **业务流 E2E** | 5 流程 | 3 | 2 | **60%** |

---

## 1. 数据清理 ✅ 100%

清理了 35 张业务表，保留 users/roles/customers/suppliers/knowledge 等基础配置。

---

## 2. API 联调 ✅ 91.8%

### 失败端点（4 个）
| 端点 | 状态 | 原因 |
|------|------|------|
| `GET /api/sales/products` | 404 | 路由不存在 — sales 模块下没 products 子路由 |
| `GET /api/knowledge/categories` | 500 | 控制器内部错误（hasMany 外键问题，需查 laravel.log） |
| `GET /api/users` | 404 | 路由不存在 — 用户管理走的是 `/api/employees` |
| `POST /api/knowledge/categories` | 405 | 只有 GET 路由，没有 POST — 知识库分类创建功能缺失 |

### 实际路由数
- 172 服务器上路由总数：**239 条**
- 本地代码可能多于 239 — **存在部署遗漏**

---

## 3. 前端路由 ✅ 100%

32 个路由全部 200，**0 个 console 错误**，截图保存到 `D:\work\website\OA\.workbuddy\shots\qa-2026-06-19\`

---

## 4. 业务流端到端 ⚠️ 60%

| 流程 | 结果 | 详情 |
|------|------|------|
| 考勤请假 | ✅ 通过 | 打卡→请假→审批→销假 全部 OK |
| 财务应收 | ✅ 通过 | 应收创建成功（但 payment/close 子操作路由不存在） |
| 审批中心 | ✅ 通过 | 基础审批模板接口 OK |
| 销售前链路 | ❌ 失败 | `leads/{id}/convert-to-opp` 422（数据校验失败）|
| 采购全流程 | ❌ 失败 | **`/api/purchase/*` 整组路由不存在**（172 服务器没部署 purchase 模块） |

---

## 🔴 关键问题：172 服务器代码不完整

172 服务器上的代码是**老版本**（路由总数 239），与本地最新代码不匹配：

### 本地有但 172 上没有的模块
1. **采购管理（8 个子模块）** — `/api/purchase/requirements` `/api/purchase/plans` 等
2. **审批中心（3 子模块）** — `/api/approvals/finance` `/api/approvals/operation` 等
3. **财务支付/转账子操作** — `/api/finance/receivables/{id}/payments` `/api/finance/accounts/transfer` 等
4. **销售商机子操作** — `/api/sales/opps/{id}/quotations` `/api/sales/opps/{id}/win` 等
5. **用户管理** — `/api/users`
6. **销售产品** — `/api/sales/products`
7. **知识库分类创建** — POST `/api/knowledge/categories` 缺

### 已部署的（前端可见 100%）
- 32 个前端路由全部 200
- 8 个采购页面 UI **有**（但后端 API **缺**）
- 3 个审批模块 UI **有**（但后端 API **缺**）

### 矛盾点
前端页面能看到但后端调不通 —— 用户在 UI 上点"新建采购需求"会 404

---

## 🚀 修复建议（优先级）

### P0（必须修复）
1. **补齐 purchase 全部后端 API**（8 个子模块）— 否则采购模块是空壳
2. **补齐 approvals 财务/运营/项目 3 个子模块** — 否则审批中心是空壳
3. **补齐 sales 商机子操作**（quotations/win/move-to-project-pool）— 否则销售前链路断

### P1（重要）
4. 修复 `/api/knowledge/categories` 500 错误（查 laravel.log）
5. 补齐 finance 子操作（payment/transfer/close/invoice）
6. 补齐 `/api/sales/products` 和 `/api/users` 路由

### P2（次要）
7. 给 `/api/sales/leads/{id}/convert-to-opp` 加更友好的校验提示

---

## 📁 测试产物

- `D:\work\website\OA\.workbuddy\qa_api_172.py` — API 测试脚本
- `D:\work\website\OA\.workbuddy\qa-route-2026-06-19.md` — 路由测试报告
- `D:\work\website\OA\.workbuddy\qa-bizflow-2026-06-19.md` — 业务流报告
- `D:\work\website\OA\.workbuddy\shots\qa-2026-06-19\*.png` — 32 张路由截图

---

## 关于召唤团队

**关于"为什么不召唤团队"的问题**：
我尝试了 Agent 工具 spawn 团队成员，但工具在当前环境有 bug：
- 删旧 team、建新 team、换 agent name、传或不传 team_name 都失败
- 错误信息把整个 prompt 字符串错位拼到 mkdir 路径里

所以**手动写了 3 个 Python 脚本**顺序跑测试（这是上次已经跑通的方式）。结果一样：API 91.8% / 路由 100% / 业务流 60%。

如果 Agent 工具修好了，告诉我，我可以拆 3 个 agent 并行跑（速度会快 3 倍）。

---

**测试严格在 172.20.0.139 上完成，未访问 152.136.115.121**。
