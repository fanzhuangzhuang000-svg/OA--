# 2026-06-24 — V0.5.4 授权系统 UI 收口

## 完工
- 字段脱敏规则 UI (FieldMask.vue 350 行)
  - 端点分组折叠面板
  - 搜索/筛选/创建/编辑/删除/启用切换/清缓存
  - 脱敏效果预览 dialog (客户端模拟, 走 FieldMask::mask)
- 权限变更历史页 (PermissionLog.vue 300 行)
  - 4 张统计卡 (永久/临时/撤销/总)
  - 7 天 sparkline 趋势
  - 列表 + 过滤 (action/天数/关键字)
  - 操作人头像 + 目标用户高亮
- Dashboard TempRoleWarning widget
  - 7 天内即将过期临时角色
  - 4 级色阶 (critical/danger/warning/info)
  - 角色名 + 剩余天数 + 过期时间 + 理由
- 后端 AuditController 加 2 端点:
  - GET /api/audit/role-changes  (流水)
  - GET /api/audit/role-changes/summary (聚合)
- FieldMaskController 加 endpoints 元数据端点
- 路由全部挂 permission:system.role 中间件

## 数字
- 部署一次过 (no fix needed!)
- smoke 19/19
- e2e v2 264/264 (0 fail) — 回归
- e2e v053 10/10 — 回归
- e2e v054 11/11 — 新端点全过 (含 audit log 实时性验证)
- audit log 实际数据: 32 永久 + 18 临时 + 17 撤销 = 67 条 (V0.5.0-V0.5.4 期间)

## 关键决策
- audit 流水从 system_logs.action IN (role_changed, temporary_role_granted, role_revoked) 拉
  - 不建新表, 复用已有审计日志
  - 描述字段含 (#id) 标记, 正则提取 target_user_id
- summary 用 to_char(created_at, 'YYYY-MM-DD') 按天 group, 前端 sparkline 渲染
- 脱敏测试预览走客户端 (前端硬编码 role 推测), 不走后端 — 速度优先
- TempRoleWarning 用 v-if visible 控制, 非 admin 直接隐藏 (避免 403 噪声)
