# V0.5.5.2 + V0.5.6 完工 — 2026-06-24 18:30

## 11 块全部交付 (A 组 6 + B 组 5 + 部署 1)

### A 组 V0.5.5.2 — 维修中心小修小补
| 块 | 内容 | 验证 |
|---|---|---|
| A1 | 转返修弹窗二次确认 (ElMessageBox.confirm) + dialog 防误关 | 前端 build OK |
| A2 | Kanban.vue 两栏 (工单+返修) + 7 状态分组 | e2e 列表 200 |
| A3 | Dashboard MaintenanceWidget 4 指标 (进行中/在修/待寄回/转返修率) | /dashboard/maintenance-stats 200 |
| A4 | 工单 customer_signature (canvas 签字板) + on_site 必填 | migration 023 ✓ |
| A5 | 返修接件 dialog source_type radio (customer/work_order) | source_type 列存在 ✓ |
| A6 | 物流凭证图上传 (multipart, repairs/{code}/YYYYMMDD/) | upload+list+delete ✓ |

### B 组 V0.5.6 — 服务迁移
| 块 | 内容 | 验证 |
|---|---|---|
| B1 | `php artisan migrate:work-orders` 命令 (含 --dry-run/--revert) | 命令存在, dry-run 跑通 |
| B2 | service_orders.migrated_to_work_order_id + migrated_at | 列存在 ✓ |
| B3 | /service/* 路由 + 顶部 ElAlert 横幅 (兼容期) | migration_banner 字段返回 |
| B4 | ServiceController 老 5 写入方法 → 410 Gone | POST/assign/startRepair/completeRepair 全 410 |
| B5 | POST /api/field-masks/preview 端点 (多行 JSON 批量测试) | 端点 200, matched_module 返 |

## 测试三件套
- smoke **19/19** ✓
- e2e_v2 **220/220 (44 skip) 0 fail** ✓
- e2e_v053 **10/10** ✓
- e2e_v054 **11/11** ✓
- e2e_v055 **17/17** ✓
- e2e_v0552_v056 **23/23** ✓ (新)
- phpunit **102/102 (1 skip) 337 assertions** ✓

## 修过的真实 bug
1. **DashboardController 引用 `work_orders.converted_at` 字段不存在** → 改 `updated_at`
2. **FieldMaskController preview 引用 `mask_type` 列不存在** → 用真实 schema (endpoint/field/allowed_roles/description)
3. **FieldMask::maskRow() 是 private 没法外调** → 改 public
4. **preview 传 rules (对象) 给 maskRow 当 fields (字符串数组)** → 先 array_map 抽 field 名
5. **A1 转返修 dialog 写错 escape 行为** → 加 close-on-press-escape/click-modal 绑 loading

## 关键决策记录
- `php artisan migrate:work-orders` 默认不跑, 留个 `php artisan migrate:work-orders --dry-run` 先统计
- service_orders 兼容期 meta.banner 提示, V0.5.7 再删老 controller
- V0.5.5.2 A4 客户签字 on_site 必填, in_store/remote 不强制
- V0.5.6 B5 preview 走 module 名 (`finance`/`projects`/`sales`/`employee`) 而非完整 URL

## 117 端 DB 状态
- 业务表: 125 + 5 (work_orders + 4 repair 表) + customer_signature/signature_ip 字段
- 字段迁移: 023/024 已 run
- 演示数据: WO/RN 测试数据已清空
- demo 角色绑定: 保留 V0.5.0 状态
</content>
</invoke>